setlocal tabstop=8
setlocal shiftwidth=8
" Keep 'hard' tabs (no conversion to spaces)
setlocal noexpandtab

setlocal cindent
setlocal autoindent

" The DPDK guide suggests 80, but up to 100 is acceptable:
setlocal textwidth=80
setlocal colorcolumn=80
